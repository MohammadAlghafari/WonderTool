import 'package:flutter/material.dart';

class WonderCubeScreen extends StatelessWidget {
  const WonderCubeScreen({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: const [],
    );
  }
}
