import 'package:flutter/material.dart';

class LoungeCafeScreen extends StatelessWidget {
  const LoungeCafeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: const [],
    );
  }
}
